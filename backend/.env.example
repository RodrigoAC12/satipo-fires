# Configuración de Base de Datos PostgreSQL
# Copia este archivo a .env y completa con tus valores

DB_USER=admin
DB_PASSWORD=admin
DB_HOST=localhost
DB_PORT=5432
DB_NAME=satipo_fires

# Nota: Si tu contraseña tiene caracteres especiales (como ñ, @, etc.)
# el código ahora los maneja automáticamente con URL encoding.
